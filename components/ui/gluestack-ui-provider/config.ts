"use client";
import { ColorVariables } from "@/constants/color_variables";
import { vars } from "nativewind";

export const config = {
  light: vars(ColorVariables.light),
  dark: vars(ColorVariables.dark),
};
